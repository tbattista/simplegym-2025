# Use the official Gotenberg image
FROM gotenberg/gotenberg:8

# Gotenberg runs on port 3000 by default
EXPOSE 3000

# The image already has the correct entrypoint configured
# No additional configuration needed for basic PDF generation
